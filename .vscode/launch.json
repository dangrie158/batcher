{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch Program",
            "module": "slurmbatcher",
            "args": [
                "${input:config}"
            ],
            "console": "integratedTerminal"
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "config",
            "default": "tests/sample.toml",
            "description": "Path to the configuration"
        }
    ]
}
