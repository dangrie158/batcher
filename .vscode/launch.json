{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch Program",
            "module": "slurmbatcher",
            "args": [
                "${input:config}",
                "--dry-run"
            ],
            "console": "integratedTerminal"
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "config",
            "default": "examples/sample.toml",
            "description": "Path to the configuration"
        }
    ]
}
